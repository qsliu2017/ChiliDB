[package]
name = "chilidb-storage"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"

[dependencies]
chilidb-common = { path = "../common" }
thiserror = "2"

[dev-dependencies]
criterion = { version = "0.8.2", default-features = false, features = ["cargo_bench_support"] }
stateright = "=0.31.0"
tempfile = "3"

[[bench]]
name = "bpm_bench"
harness = false
